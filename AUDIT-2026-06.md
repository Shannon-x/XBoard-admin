# XBoard-admin 前端全面审计报告

> **审计范围**：`/Users/shuhao/Desktop/开发项目/XBoard-admin` 全部 60 个 `.vue`/`.js` 源文件，约 22,983 行
> **执行方式**：6 维度并行审计员 → 132 条原始发现 → 132 条对抗式逐条复审 → 完整性批评 → 6 项缺口二次扫描
> **统计结果**：121 条经对抗复审确认的有效发现，8 条被驳回为误报，3 条复审超时
> **本报告中所有 file:line 引用均可点击跳转**

---

## 一、执行摘要

XBoard-admin 在功能宽度上完成度很高（19 个管理页、15 个 service、Pinia store 全覆盖），但因为缺少 TypeScript / 单元测试 / 接口契约校验，**几类系统性风险积累得很厚**：

1. **认证设计是当前最大风险面**：管理员令牌明文写进 `localStorage` 且路由守卫只检查"localStorage 里有没有这个 key"，意味着任意 XSS 都能直接拿走整个会话，注销 / 后端踢人都无法立即生效。
2. **"破窗"型 Bug 仍在生产中**：公告删除按钮**点击确认后什么都不做**、公告排序保存抛 TypeError、NodeConfigDialog 里 6 个图标按钮像之前 `CopyDocument` 那样**完全不渲染**、Knowledge 排序对话框显示空白行。这些都是用户能立刻碰到的硬伤。
3. **后端契约不对齐**：与上次修复的 PlanSave 同类型问题至少还有 7 处（saveManagedNotice 发布 boolean 而后端期望 1/0、generateCoupons 不发 `show` 导致隐藏被覆盖、payment.save 丢 `show/sort/notify_url` 等）。

下面按"必修 → 应修 → 可选"三档展开。每条都给了精确的 file:line 和落地建议。

---

## 二、关键统计

| 维度 | Critical | High | Medium | Low | Info | 合计 |
|------|:-:|:-:|:-:|:-:|:-:|:-:|
| 安全 (security) | 0 | 3 | 1 | 10 | 1 | **15** |
| Bug | 0 | 2 | 22 | 18 | 0 | **42** |
| 功能 (functional) | 0 | 2 | 0 | 0 | 0 | **2** |
| 视觉/UX (ux) | 0 | 2 | 8 | 14 | 1 | **25** |
| 性能 (perf) | 0 | 1 | 4 | 10 | 0 | **15** |
| 质量 (quality) | 0 | 3 | 13 | 6 | 1 | **22** |
| **合计** | **0** | **13** | **48** | **58** | **3** | **121** |

> Critical = 0 不代表"没风险"，而是没有"远程未授权直接命中"型漏洞。最严重的 SEC-001/SEC-002 属于 High：触发它们要先注入 XSS 或先拿到管理员账号，但一旦中招杀伤范围是整张会话。

---

## 三、必须立即修复（High & 用户立刻能踩坑的）

### 🔴 SEC-001 — 管理员凭据以明文存于 localStorage，任意 XSS 即可窃取
- **位置**：[src/services/auth.js:30-36](src/services/auth.js#L30-L36)（写入）·[src/services/api.js:71-86](src/services/api.js#L71-L86)（每次请求都拿 `authData` 作为 Authorization 头）
- **问题**：`persistAuthSession` 把完整会话（包含被后端当成 `Authorization` 头使用的 `authData` 字符串）写到 `localStorage['ltc-admin-auth']`。同源任意脚本（包括 SEC-002 的 v-html 注入）都能读出来。
- **影响**：任何 DOM 级注入 → 完整管理员账号接管。
- **修复方向**：
  - 优选：让后端把会话改用 `HttpOnly + Secure + SameSite=Strict` 的 cookie 下发，前端不再持有 token。
  - 次选：把 token 仅放在 JS 模块级变量里（刷新页面就要求重新登录），并实现一个 `/refresh` 短令牌循环。
  - 兜底：至少把 `localStorage` 换成 `sessionStorage`，限定 Tab 级生命周期。
- **工作量**：大（涉及后端配合）

### 🔴 SEC-002 — 插件 README 走 `v-html` 但未做 HTML 净化（存储型 XSS）
- **位置**：[src/views/PluginsPage.vue:13](src/views/PluginsPage.vue#L13)（`import { marked }`）· [PluginsPage.vue:58-66](src/views/PluginsPage.vue#L58-L66)（`renderedReadme` 计算属性）· [PluginsPage.vue:657](src/views/PluginsPage.vue#L657)（`v-html="renderedReadme"`）
- **问题**：`marked` ≥ 7 不再内置 XSS sanitizer。README 内容来自插件目录（理论上可由其他管理员/开发者投放），未经任何过滤直接 v-html。grep `DOMPurify` 全仓返回零结果。
- **影响**：存储型 XSS → 与 SEC-001 链路联动 = 完整账号接管。
- **修复**：
  ```js
  import DOMPurify from 'dompurify'
  const renderedReadme = computed(() =>
    DOMPurify.sanitize(marked.parse(readmePlugin.value.readme, { breaks: true }))
  )
  ```
- **工作量**：小（装一个依赖、改一行）

### 🔴 SEC-GAP-002 — 路由守卫只检查"localStorage 里有没有 key"，被撤销的 token 仍能通过
- **位置**：[src/router/index.js:285-303](src/router/index.js#L285-L303) · [src/services/auth.js:46-49](src/services/auth.js#L46-L49)
- **问题**：`hasStoredAuthSession` 等价于 `Boolean(session?.authData)`，只要 `localStorage['ltc-admin-auth']` 不为空就算"已登录"。即使后端把会话踢掉、所有请求都返回 401，守卫依然放行；用户看到的是"页面打开但什么都加载不出来"。
- **修复**：让 `auth.js` 在 `requestDashboardApi` 收到 401/403 时调用 `authStore.logout()` 并 `router.push('/login')`；守卫改成读 `authStore.isAuthenticated`（响应式）。`main.js` 启动后可顺便探一次 `passport/auth/check` 验活。
- **工作量**：小~中

### 🔴 BUG-001 / FUN-001 / UX-001 — 公告"删除"按钮**确认后什么都不做**
- **位置**：[src/views/NoticesPage.vue:119-131](src/views/NoticesPage.vue#L119-L131)
- **问题**：`handleDelete` 函数体只有 `ElMessageBox.confirm`，**根本没调任何 service / store**，没有 toast，没有 reload。用户点垃圾桶 → 弹窗 → 确认 → 公告还在。三个维度审计员独立发现同一问题，对抗复审全部确认。
- **修复**：
  ```js
  async function handleDelete(notice) {
    try {
      await ElMessageBox.confirm(`确定删除公告"${notice.title}"吗？`, '删除公告', { type: 'warning' })
      await deleteManagedNotice(notice.id)   // 已在 services/notices.js 导出
      ElMessage.success('公告已删除')
      await loadNotices()
    } catch (err) {
      if (err !== 'cancel') ElMessage.error(err?.message || '删除失败')
    }
  }
  ```
- **工作量**：极小

### 🔴 BUG-002 — 公告排序保存调用一个**不存在的** store 方法 → 抛 TypeError
- **位置**：[src/views/NoticesPage.vue:133-142](src/views/NoticesPage.vue#L133-L142)
- **问题**：`adminStore.sortManagedNotices(ids)` —— 但 `src/stores/admin.js` 从未 import / wrap / 暴露 `sortManagedNotices`。每次保存排序都 `TypeError`，被外层 `catch` 包装成模糊的"排序保存失败"。
- **修复**：直接在 NoticesPage 内 `import { sortManagedNotices } from '../services/notices'`，或在 store 加一个 thin wrapper（与 `saveManagedNoticeItem` 模式一致）。
- **工作量**：极小

### 🔴 UX-002 — `NodeConfigDialog` 6 处 `:icon=` 引用未导入的变量 → 按钮渲染为空白
- **位置**：[src/components/nodes/NodeConfigDialog.vue:1101](src/components/nodes/NodeConfigDialog.vue#L1101) · [:1571](src/components/nodes/NodeConfigDialog.vue#L1571) · [:1597](src/components/nodes/NodeConfigDialog.vue#L1597) · [:1859](src/components/nodes/NodeConfigDialog.vue#L1859) · [:2155](src/components/nodes/NodeConfigDialog.vue#L2155) · [:2167](src/components/nodes/NodeConfigDialog.vue#L2167)
- **问题**：模板里写了 `:icon="Delete"` / `:icon="Key"` / `:icon="Refresh"`，但文件顶部只 import 了 `Trash2 / RefreshCw / KeyRound / GripVertical / ArrowUp / ArrowDown / X`。`Delete / Key / Refresh` 在 setup 范围内都是 `undefined` → `<el-button :icon="undefined" />` 渲染成一个空方块。**与你前几小时刚修的 `CopyDocument` 是同一类 bug**。
- **影响**：节点配置对话框里"删除动态规则"、"生成 Reality 私钥对"、"生成 Short ID"、"删除 fallback 子节点"、Cert 配置弹窗的两个生成按钮全都是**完全看不见的空按钮**。
- **修复**：批量替换为已导入的 `Trash2 / KeyRound / RefreshCw`；同时给这些按钮加可见文字 label（"删除"/"生成"），即使图标再坏也不会消失。
- **工作量**：极小

### 🔴 FUN-005 — Knowledge 排序对话框**显示空白行**（字段名约定不一致）
- **位置**：[src/views/KnowledgePage.vue:267-272](src/views/KnowledgePage.vue#L267-L272)
- **问题**：`SortDialog` 约定 `items: Array<{ id, name }>` 并渲染 `{{ item.name }}`（[SortDialog.vue:7,74](src/components/common/SortDialog.vue#L7-L74)），但 article 对象只有 `title`。开发者看不到自己在拖谁。Compare：NoticesPage 用对了 `articles.map(n => ({ id: n.id, name: n.title }))`。
- **修复**：把 KnowledgePage 那一行改成 `:items="articles.map(a => ({ id: a.id, name: a.title }))"`。
- **工作量**：极小

### 🔴 PER-001 — Vite 配置完全空白，主 bundle 把 Monaco / md-editor / marked 全打进去
- **位置**：[vite.config.js:1-6](vite.config.js#L1-L6) · [src/router/index.js:5-22](src/router/index.js#L5-L22)（19 个 view 全部静态 import）
- **问题**：当前 `dist/assets/index-*.js` ≈ 2 MB，仅一个用户登录就要全量下载 Monaco/marked/md-editor。没配 manualChunks，没配 lazy route。
- **修复**：
  ```js
  // vite.config.js
  export default defineConfig({
    plugins: [vue()],
    build: {
      target: 'es2020',
      rollupOptions: {
        output: {
          manualChunks: {
            'vendor-element': ['element-plus'],
            'vendor-editor': ['monaco-editor', '@monaco-editor/loader', 'md-editor-v3', 'marked'],
            'vendor-vue': ['vue', 'vue-router', 'pinia', 'vue-i18n'],
          },
        },
      },
    },
  })
  ```
  以及把 router 里的 19 个 view 改成动态 import：`component: () => import('../views/PluginsPage.vue')`。
- **工作量**：中

### 🔴 QUA-003 — 8 个 service 绕开 `requestDashboardMutation`，手写同一份 fetch wrapper
- **位置**：[src/services/payment.js:38-48](src/services/payment.js#L38-L48) · 同类还有 `coupons.js / knowledge.js / giftcards.js / tickets.js / users.js / orders.js / nodes.js`；中心实现在 [src/services/api.js:122-173](src/services/api.js#L122-L173)
- **问题**：`requestDashboardMutation` 集中实现了 401/403/422/code envelope 的判断，但 ~20 处 mutation 都自己 `fetch + headers + ok + json` 重写一遍，错误消息各不相同，401 处理也飘移。
- **影响**：上面 SEC-GAP-002 的"401 不退出"问题之所以难根除，根因就是各 service 各干各的。
- **修复**：把所有 mutation 改用 `requestDashboardMutation(apiUrl, body)`；需要自定义错误就在调用方 try/catch。估算可删 ~250 行。
- **工作量**：中

### 🔴 GAP-002 — OrdersPage 的"分配订单"对话框**无任何校验**
- **位置**：[src/views/OrdersPage.vue:489-516](src/views/OrdersPage.vue#L489-L516) · [:177-192](src/views/OrdersPage.vue#L177-L192)
- **问题**：`<el-form :model="assignForm">` 没 `ref` 没 `rules`，每个 form-item 也没 `required`/`prop`。`submitAssign()` 直接 POST `Math.round(Number('') * 100) = 0`，会真的给用户开一张 0 元订单 / 或带 `NaN`。
- **修复**：
  ```js
  const formRef = ref()
  const assignRules = {
    email:       [{ required: true, type: 'email', message: '请输入合法邮箱' }],
    planId:      [{ required: true, type: 'number', message: '请选择套餐' }],
    period:      [{ required: true, message: '请选择周期' }],
    totalAmount: [{ required: true, type: 'number', min: 0, message: '金额必须为非负数字' }],
  }
  async function submitAssign() {
    await formRef.value.validate()
    // ...
  }
  ```
- **工作量**：小

---

## 四、应当近期修复（Medium，共 48 条，按类别归并）

### 安全（Medium）
- **GAP-001（多处）**：401/403 响应不会清会话也不会跳登录 → 进入"看上去登录但啥也加载不出来"的死循环。集中在 [src/services/api.js:93,133,185](src/services/api.js#L93)。需要在 `requestDashboardApi/Mutation` 内部接入 authStore.logout()。

### 功能/UX
- **FUN-002** [TicketsPage.vue:385-392](src/views/TicketsPage.vue#L385-L392)：**优先级筛选标签是装饰品**，点击没有任何效果。要么删掉要么真正接上 filter。
- **FUN-003 / UX-006** [TicketsPage.vue:101-104](src/views/TicketsPage.vue#L101-L104)：搜索框 placeholder 写"搜索工单标题或用户邮箱"，但 loader 只传 `email` 参数，标题搜索永远无效。
- **FUN-007**：以下提交按钮**全部缺 `:loading`**，双击会重复提交：
  - [PaymentPage.vue:321](src/views/PaymentPage.vue#L321)
  - [KnowledgePage.vue:263](src/views/KnowledgePage.vue#L263)
  - [GiftCardsPage.vue:189,202](src/views/GiftCardsPage.vue#L189)
  - [NodeConfigDialog.vue:1974](src/components/nodes/NodeConfigDialog.vue#L1974)
- **UX-007** [UsersPage.vue:525-532](src/views/UsersPage.vue#L525-L532)：顶部"导出 CSV"按钮的 scope 写死 `'all'`，**完全无视当前筛选**。视觉上又紧贴筛选条件 → 用户会以为是"导出筛选结果"。要么改成发送 `buildFilterArray()`，要么明确按钮文案为"导出全部用户"。
- **UX-009** [GiftCardsPage.vue:172-181](src/views/GiftCardsPage.vue#L172-L181)：让管理员**手工输入套餐 ID 数字**（而不是选择器）。改成 `<el-select>` 列出 plans。

### Bug（高频项）
- **GAP-002 / GAP-003 / GAP-004** [UsersPage.vue:234-293](src/views/UsersPage.vue#L234-L293) · [OrdersPage.vue:73-135](src/views/OrdersPage.vue#L73-L135) · [TicketsPage.vue:84-128](src/views/TicketsPage.vue#L84-L128)：列表型页面 `loadXxx()` **无 seq token / AbortController**。快速切筛选/翻页时旧响应会覆盖新响应，UsersPage 还会把错的 snapshot 持久化到 sessionStorage，下次加载就是错的。
  - 修复模式：参照刚才 TicketsPage 里 `loadTicketUser` 用的 `ticketUserFetchToken` 写法，给每个 loader 加一个 monotonic seq。
- **BUG-005** [src/stores/admin.js:1246-1268](src/stores/admin.js#L1246-L1268)：`loadIncomeOverview` 在 await 前就改了 range 状态，且不带 token 守卫 → 区间快速切换时图表数据会错位。
- **BUG-006** [NodesPage.vue:431-434](src/views/NodesPage.vue#L431-L434)：`abnormalOnly` 过滤条件检查 `node.status === '高负载'/'维护中'`，但 `resolveStatusLabel` 词汇表里**根本没有这两个字符串** → 这个过滤永远命中 0。
- **BUG-009** [PlansPage.vue:156-165](src/views/PlansPage.vue#L156-L165)：行内开关切换后**强制 reload 整张列表** → 两个开关同时切就会丢一个状态。改成乐观更新（参考 NodesPage handleShowToggle）。
- **GAP-003 (notices)** [services/notices.js:148-154](src/services/notices.js#L148-L154)：`saveManagedNotice` 把 `show` 当 boolean 发，**其它 service 全发 1/0**，后端有概率把 false/true 当字符串存进 DB。
- **GAP-007 (coupons)** [services/coupons.js:86-115](src/services/coupons.js#L86-L115)：`generateCoupons` 在编辑模式下不发 `show`，导致用户保存后**所有隐藏的优惠券被默默重新开放**。
- **FUN-004** [UsersPage.vue:473-485](src/views/UsersPage.vue#L473-L485)：`copyLoginUrl` 在 `if (!user) return` 之前就解了 `user.token` —— 用户对象为空时直接 TypeError。同时 URL 写死 `${origin}/#/login?token=...` **不带 frontendSecurePath 前缀**，在带前缀部署上点击就 404。
- **GAP-005 / GAP-006**：表单依赖装饰性 `required` 而**没有 `el-form :rules` + `formRef.validate()`**。涉及 UsersPage / OrdersPage / PaymentPage / GiftCardsPage / NoticeEditorDialog / KnowledgePage 等 9 个对话框（详见原始数据）。

### 性能（Medium）
- **PER-002** [NodesPage.vue:206-236](src/views/NodesPage.vue#L206-L236)：30s 自动轮询**不检查 `document.visibilityState`** 也不检查上一次请求是否还在飞，Tab 在后台也疯狂打请求。
- **PER-004** [PluginsPage.vue:452-457](src/views/PluginsPage.vue#L452-L457)：插件搜索框**每次输入都打一次全量列表请求**（无 debounce）。
- **PER-005** [PluginsPage.vue:253-269](src/views/PluginsPage.vue#L253-L269)：`loadPluginTypes` 串行 await 后才开始 `loadPlugins` → 首屏时长翻倍。
- **PER-006** [DashboardPage.vue:64-72](src/views/DashboardPage.vue#L64-L72)：mount 时一次性并行打 7 个 API 调用，无缓存无 stagger；切回 dashboard 又来 7 个。

### 质量（Medium）
- **QUA-001** [src/services/api.js:140](src/services/api.js#L140)：`console.log` 调试输出**到了生产**，每次请求都打日志（且部分日志含用户邮箱）。
- **QUA-002**：`formatTimestamp` / `formatBytes` 在 5+ 个 service 里被重复实现。抽 `src/utils/format.js`。
- **QUA-004** [src/stores/admin.js:203-207](src/stores/admin.js#L203-L207)：`const users = []` / `const tickets = []` 是早期假数据残留 store 一直在 return。可以直接删。
- **QUA-006**：[NodeConfigDialog.vue](src/components/nodes/NodeConfigDialog.vue)（2566 行）/ [NodesPage.vue](src/views/NodesPage.vue)（2113 行）/ [stores/admin.js](src/stores/admin.js)（1855 行）都是单文件巨兽。应按 protocol 拆 NodeConfigDialog、按域拆 admin store（auth / nodes / users / billing / dashboard 五个 module）。
- **QUA-010**：**零测试 / 零 TS**：23k 行无任何自动化保障。是上面所有 Bug 类系统性发生的根因。
- **QUA-011** [UsersPage.vue:466](src/views/UsersPage.vue#L466) 等 3 处：已经有 `CopyButton` 复用组件，但仍在手写 `navigator.clipboard.writeText().then(...)`。
- **QUA-013 / GAP-002 (quality)** [NodeGroupsPage / NodeRoutesPage / ThemeConfigPage]：三个最新的 view **完全没 import useI18n**，76 行硬编码中文。要么走 i18n，要么集体撤掉 vue-i18n 依赖。

---

## 五、可选优化（Low / Info，按类别简列）

### 安全（10）
- 开放重定向：`?redirect=` 未校验是否同站
- `window.open(..., '_blank')` 全仓 5 处都没加 `noopener,noreferrer`
- `console.log` 每次请求都打用户邮箱（QUA-001 重叠）
- 插件上传 `accept=` 只是 hint，没做 client 端 MIME/size 校验
- 仓库根目录有硬编码 example credentials（`README` 示例）
- `index.html` 无 CSP / X-Frame-Options / Referrer-Policy
- Authorization 头是 raw blob，没有 `Bearer` 前缀也没有 scope
- `frontend_custom_html` 配置值原样下发到客户端 → 客户端面临存储 XSS（前端是受信场景，仍建议加 sanitize）

### Bug（18）
- 多个对话框关闭时不重置 state（GiftCards 模板对话框、UsersPage edit 等）
- `users.js fetchManagedUsers` 用 `||` 兜底分页，`total: 0` 被当成 missing
- 节点 `online` 字段归一化时丢真值
- `NodeConfigDialog` 表单 prop 数组按引用复用，触发跨实例污染
- `IncomeOverviewCard` 翻译只在 setup 时取一次，切语言后 metricOptions 不更新
- pageSize 切换后 page 没回到 1 → 落到 `page > totalPages` 空页
- `togglePaymentShow` 命名暗示 show，实际 endpoint 是 enable
- `createSafeSettingsPayload` 对 secret 字段 `.trim()` → 故意带空格的 secret 会被默默改写
- 19 个 view import 了 `useI18n()` 解构 `t` 但**从不调用** —— bundle 浪费 + 误导后续 RD

### 视觉/UX（14）
- 表格行点击进详情但**无 hover / cursor 提示**（TicketsPage）
- 仅图标按钮无 `aria-label` / `title`（编辑、删除）
- MetricsGrid 所有卡都触发 click，但只有 3 张有 `is-clickable` 视觉
- `SortDialog` 拖拽只有 ⠿ 字符提示，**无键盘备选**（违反 WCAG）
- 通用错误提示只显示"失败"，吞掉原因（多处）
- 表格列总宽 1400px，窄屏被 fixed=right 列遮挡
- 多个 dialog 固定宽度，<768px 溢出
- 一些 placeholder 仍是英文（混排）

### 性能（10）
- NodesPage 三个 computed（filteredNodes / stats / statusSummary）各自跑全表一次，可合并
- 节点 deep watcher 每次轮询深拷贝整张表
- `cache-buster` query param 让浏览器 HTTP cache 完全失效
- `marked.parse` 在 computed 里同步跑大段 README，无 memo
- `chartSeries` computed 每次 reactive read 都重新分配 6 个数组

### 质量（6）
- 160+ 处内联 style，应入 CSS 模块
- 无全局错误边界（uncaught render error 直接白屏）
- `buildSecureV2ApiUrl` 与 `buildDashboardApiUrl` 实际逻辑等价 → 二选一
- `resolveMessage()` 的中文兜底字典实际永远不会被命中（dead code）
- 某文件中段写 `import { computed } from 'vue'`，违反 module-top 规则

---

## 六、系统性建议

| 主题 | 建议 | 预期收益 |
|------|------|---------|
| **认证基础设施** | 把 SEC-001 / SEC-GAP-002 / GAP-001 一起改：cookie-based session + 401 自动登出 + 路由守卫走 `authStore.isAuthenticated` | 一次性消除认证面 80% 的高危项 |
| **统一 HTTP 层** | 强制所有 mutation 走 `requestDashboardMutation`；service 不再裸 `fetch` | 集中 401 处理 / 错误展示 / 日志埋点；预计减 250 行重复代码 |
| **表单校验骨架** | 全仓 dialog 改成 `formRef + :rules + validate()`，淘汰 `if (!x) ElMessage.warning` 这种散装写法 | 一次修复所有"未填必填字段也能提交"的 Bug 类 |
| **API 契约校验** | 用 `zod` 给每个 service 的 response 加 schema parse；不匹配就在 dev 控制台 warn。同时 backend 加合约测试，前端发 `show:true`/`show:1` 之类的差异立刻显形 | 根治 PlanSave/Notice show 字段那类"前端发了后端默默丢弃"的 Bug 类 |
| **抽公共组件** | 已经有 `CopyButton` 这种好范式。继续抽：`<FormDialog>`（带 loading/validate）、`<ConfirmDestroy>`（统一确认+毁坏色）、`<JsonInput>`（用于 NodeRoutes 那种 JSON 字段，自动 parse/lint） | 单点修复 → 全仓收益 |
| **代码切分** | router 改 dynamic import + Vite manualChunks（PER-001） | dist 体积、首屏时延、缓存命中率全面改善 |
| **拆超大文件** | NodeConfigDialog 按 protocol 拆 6 个子组件；admin.js 按域拆 5 个 module | 单文件 < 600 行，新人能 onboard |
| **加测试地基** | 至少给 services/ 加 vitest 单测（覆盖 normalize 函数 + payload 形状），再给 stores/ 加几个关键 action 的单测 | 前面所有 Bug 类的"防止复发" |
| **i18n 表态** | 要么补齐 NodeGroupsPage/NodeRoutesPage/ThemeConfigPage 的翻译，要么撤掉 vue-i18n。当前半挂半就的状态最糟 | 维护成本立刻下降 |

---

## 七、复审与可信度说明

本报告由独立的 6 个维度审计员各自扫描 + 132 条**对抗式逐条复审**（默认怀疑、要求引证当前文件原文）+ 完整性批评 + 6 项缺口二次扫描产出。

- 132 条原始发现 → 121 条被复审确认（91.7%）
- 8 条被复审**判定为误报**（已剔除）
- 3 条复审超时（已保守剔除）

**未被覆盖的领域**（坦白说明）：

- 没有运行时动态分析，所有发现都基于静态阅读
- 没有跑实际的负载/性能 profiling，PER 类是基于代码模式的推断
- 后端 PHP 部分仅在涉及前端契约的位置点到为止，不在本次审计范围
- 移动端 UI 仅按现有媒体查询点检，未真机测试

---

**审计完成时间**：约 27 分钟 · 147 个 agent · 297 万 tokens · 1342 次工具调用
**优先级建议**：把"必须立即修复"那 10 条全做了大约需要 1-2 个工作日，能消掉用户能直接感知的 80% 风险。
